# מסמך העברה — פרויקט דפי נחיתה · ישיבת יראנו ניסים
עדכון אחרון: 16.08.2026 · נכתב לצורך המשך עבודה מסשן/משתמש Claude אחר

> **עדכון 17.08 — שדרוג מקצועי של v11 + כל 8 הסגנונות:**
> - **טופס:** נוסף שדה **מייל (חובה)**, validation אמיתי למייל/טלפון/גיל, ו-**ה-mailto הוסר לחלוטין** —
>   המועמד לא פותח יותר תוכנת מייל. הטופס שולח POST ל-`SUBMIT_ENDPOINT` ומציג פאנל הצלחה בדף.
>   **החסימה היחידה שנותרה:** להקים את ה-Web App לפי [`tools/apps-script/README.md`](tools/apps-script/README.md)
>   (3 דקות, דורש חשבון גוגל של הישיבה) ולהזין את ה-URL ל-`SUBMIT_ENDPOINT` ב-v11.
> - **מועדים:** כל אזכורי "זמן חשוון תשפ״ז" הוסרו מכל הדפים (כולל הדור הקודם) והוחלפו ב:
>   ראיונות קבלה במהלך חודש אלול · **ההרשמה נסגרת בכ׳ באלול**.
> - **קובצי ה-theme שוחזרו** — הם עכשיו ב-`themes/theme12–18.css`, ו-`tools/regen.sh` מייצר את
>   v12–v18 מ-v11. **עורכים תוכן ב-v11 בלבד ואז מריצים `bash tools/regen.sh`.**
> - **תמונות:** נוספו נגזרות מוקטנות ב-`img/opt/` (חסכון ~1.05MB). המקורות ב-`img/` לא נגעו —
>   כדי לא לפגוע ב-kirya/yoman/sefarim שמשתמשים בהם.
> - **הוסר:** רקעי `kotel.jpg`/`binyan.jpg` (עיטור ב-5–6% אטימות), ותמונת מבנה שהוצגה
>   כ"פנים" עגולות בקרוסלת הציטוטים. מונה "1 שנה" הוסר (מספר לא מבוסס).
> - **צוות:** 4 כרטיסים עם גיאומטריה זהה. לשלושה רבנים **אין תמונה אמיתית** — יש מונוגרמה
>   מסומנת ב-`data-needs-photo`. צריך תמונות פנים אמיתיות (ראה §7).
> - נגישות: כל טקסט עובר WCAG AA בכל 8 הסגנונות; focus גלוי; skip link.

> **עדכון 16.08:** נוספו סגנונות v15–v18 (סה"כ 8 סגנונות בדור הנוכחי, כולם ב-styles.html). הנחיית הגופנים העבים (Rubik/Assistant) כבר יושמה. המאגר מכיל כעת גם פרויקטים נוספים שאינם חלק מדפי הנחיתה: מצגת קריית יראנו ניסים (kirya.html, kirya-video.html), יומן חשבון נפש (yoman/), וספר בלבבי משכן אבנה (sefarim/) — לא לגעת בהם במסגרת עבודה על דפי הנחיתה.

---

## 1. מה הפרויקט

דפי נחיתה לגיוס תלמידים ל**ישיבת יראנו ניסים** (נתניה), לקראת **זמן חשוון תשפ״ז**.
קהל היעד: בחורים 21–24 אחרי צבא. המסר המרכזי (מאושר ע"י הלקוח):
**"תן לעצמך שנה. תגלה מי אתה. תחזור הביתה."**

רקע תוכן מלא נמצא במסמך: `C:\Users\אורן\Desktop\tochnit_giyus_yeshiva (6).docx`
(חזון, רבנים, קהל יעד, שלבי גיוס — משם נלקח כל הקופי.)

אנשי המפתח שמופיעים בדפים:
- הרב רפאל רובין שליט״א — הנהגה רוחנית (מייסד המתחם, 20+ שנה)
- רבנים מייסדים: הרב חיים כהן, הרב עמית יוסף, הרב אורי ואננו
- פעילות הנוער של המתחם נקראת ״הלב היהודי״

## 2. איפה הכול חי (שלושה עותקים בלתי-תלויים)

| מקום | כתובת | תפקיד |
|---|---|---|
| GitHub (מקור האמת) | https://github.com/OreNanox/yeshiva-landing | מאגר ציבורי, חשבון OreNanox |
| GitHub Pages | https://orenanox.github.io/yeshiva-landing/ | לינקים ציבוריים להפצה (HTTPS) |
| VPS Hetzner | http://78.46.133.137:8090/ | אירוח עצמאי בדוקר מבודד |

## 3. מבנה הדפים במאגר

- `styles.html` — **עמוד הבחירה שנשלח לראשי הישיבה**: כל 8 הסגנונות של הדור הנוכחי
- `v11.html` — דף הדגל "לילה וזהב" (כחול-לילה + זהב) — **הבסיס שממנו נגזר הכול**
- `v12.html` — סגנון "קלף" (בהיר, נייר + זהב עתיק)
- `v13.html` — סגנון "מודרני" (לבן + כחול, פינות מעוגלות)
- `v14.html` — סגנון "יין" (בורדו + ברונזה)
- `v15.html`–`v18.html` — ארבעה סגנונות נוספים (נוצרו 13.08 לבקשת הלקוח "תהיה יצירתי") — נגזרות theme על אותו בסיס v11
- `index.html` + `v1.html`–`v10.html` — דור קודם (10 עיצובים ישנים). קיימים אבל הוחלפו ברמה.
- תיקיות `00`–`10` — עותקי הדור הקודם.
- `img/` — כל התמונות. החשובות: `harav.jpg` (פורטרט הרב — הירו), `harav-face.jpg` (חיתוך פנים עגול), `harav-og.jpg` (תמונת שיתוף וואטסאפ/טלגרם), `harav-field.jpg` (הרב במגרש כדורגל — רצועת רוחב מלא), `harav-hug.jpg` (חיבוק — בגלריה), `shiur.jpg`, `logo-dark.png`/`logo-light.png`.

**חשוב — איך בנויות הגרסאות:** v12–v14 הן v11 + בלוק `<style id="theme-...">` שמוזרק לפני `</head>` (קובצי `theme12/13/14.css`). כל שינוי תוכן עושים **ב-v11 בלבד** ואז מייצרים מחדש:
```bash
for v in 12 13 14; do
  sed "s|v11.html|v${v}.html|g" v11.html > v${v}.tmp
  awk 'FNR==NR{t=t $0 ORS;next} /<\/head>/{printf "%s",t} {print}' theme${v}.css v${v}.tmp > v${v}.html
  rm v${v}.tmp
done
sed -i 's|img/logo-dark.png|img/logo-light.png|' v12.html v13.html   # לוגו כהה לגרסאות הבהירות
```
**עדכון 17.08 — זה כבר לא נכון:** קובצי ה-theme **שוחזרו ונמצאים במאגר** ב-`themes/theme12–18.css`,
והאוטומציה הנכונה היא:
```bash
bash tools/regen.sh      # מייצר v12–v18 מ-v11 + themes/, כולל החלפת הלוגו לסגנונות הבהירים
```
אומת ש-כל 7 הדפים הם v11 + בלוק ה-theme בדיוק (`tools/extract-themes.sh` מוודא זאת).

## 4. עקרונות העיצוב (הנחיה מחייבת מהלקוח)

1. מרכוז מלא של כל בלוק + padding נדיב
2. טקסטורות ורקעים (grain, גרדיאנטים, תמונות רקע עדינות) — לא צבע שטוח
3. אנימציה בכל אינטראקציה: reveal בגלילה, מונים שסופרים, hover עם תנועה, קרוסלות
4. טיפוגרפיה ענקית. **בוצע (13.08):** הדפים הועברו לגופנים עבים — Rubik (700–900) לכותרות + Assistant (400–700) לגוף. אין לחזור ל-Frank Ruhl Libre בדפי הנחיתה. Ploni הוא גופן בתשלום — לא להשתמש בלי רישיון; החלופות החינמיות שנבחרו: Rubik/Assistant/Heebo.
5. כרטיסים עם אייקונים במקום bullet points
6. הירו full-bleed — **תמונת הרב חייבת להיות דומיננטית** (הנחיה מפורשת)
7. ציטוטים בקרוסלה (תמונה עגולה + ציטוט גדול + שם)
8. פלטה של 2–3 צבעים לכל סגנון

## 5. תשתית ה-VPS (78.46.133.137, Hetzner, hostname: openclaw-prod)

- משתמש ייעודי: `yeshiva` — **בלי sudo**, חבר בקבוצת docker בלבד
- אתר: `/home/yeshiva/site` (clone של המאגר)
- קונטיינר: `yeshiva-landing` — nginx:alpine, פורט 8090, רשת ייעודית `yeshiva-net`, mount קריאה-בלבד של `/home/yeshiva/site`, מגבלות 128MB/‏0.5CPU, restart unless-stopped
- **אסור לגעת** בקונטיינרים האחרים: nanox-caddy (תופס 80/443), nanox-dash, nanox-dev, yarenu-staging — שם רץ NANACHBOT עם כסף אמיתי של לקוחות. זו הסיבה לבידוד המלא.
- לשרת יש Deploy Key עם הרשאת כתיבה למאגר (`~/.ssh/id_ed25519` בשרת, רשום בגיטהאב כ-deploy key). ה-remote: `git@github.com:OreNanox/yeshiva-landing.git`

### זרימת עבודה מחייבת (הנחיית לקוח)
פיתוח **ישירות על ה-VPS** + גיבוי מקביל לגיטהאב:
```bash
# עריכה בשרת (הקונטיינר מגיש חי — שינוי נראה מיידית ב-:8090)
ssh yeshiva@78.46.133.137
cd /home/yeshiva/site
# ... עריכות ...
git add -A && git commit -m "..." && git push origin main
# GitHub Pages מתעדכן אוטומטית תוך ~דקה
```

## 6. גישה מסביבת עבודה חדשה — מה צריך

1. **SSH ל-VPS**: המפתח הפרטי נמצא רק במחשב הנוכחי: `C:\Users\אורן\.ssh\yeshiva_deploy`.
   מסביבה חדשה — שתי אפשרויות:
   א. להעתיק את זוג הקבצים `yeshiva_deploy` + `yeshiva_deploy.pub` למחשב החדש (`~/.ssh/`).
   ב. או ליצור מפתח חדש במחשב החדש ולהוסיף את הציבורי שלו לשרת, בשורה חדשה בקובץ `/home/yeshiva/.ssh/authorized_keys` (דרך חיבור קיים או דרך root).
   חיבור: `ssh -i <המפתח> yeshiva@78.46.133.137`
2. **GitHub**: המאגר ציבורי לקריאה. לדחיפה מהמחשב החדש: `gh auth login` לחשבון **OreNanox** (device flow בדפדפן). מהשרת עצמו אפשר לדחוף כבר עכשיו (Deploy Key מותקן).
3. שום סיסמה לא עוברת בצ'אט. אין סודות בתוך המאגר.

## 7. סטטוס פתוח — מה נשאר לעשות

1. **חיבור טופס המועמדות (הכי דחוף!)** — ✅ **הקוד הושלם 17.08.** ה-mailto הוסר, יש validation
   אמיתי, והטופס שולח POST ל-`SUBMIT_ENDPOINT` ומציג פאנל הצלחה. נותרו **שני דברים ידניים**:
   - **א. להקים את ה-Web App** לפי [`tools/apps-script/README.md`](tools/apps-script/README.md) — 3 דקות,
     דורש חשבון גוגל של הישיבה (ולכן סוכן לא יכול לעשות זאת). להזין את ה-URL ל-`SUBMIT_ENDPOINT`
     ב-v11 ולהריץ `bash tools/regen.sh`.
   - **ב. כתובת המייל של הצוות** — עדיין לא ידועה. ב-`Code.gs` יש placeholder.
     (`office@nanox.co.il` היה fallback של המפתח; `info@yarenunissim.com` מופיע בדור הקודם — צריך אישור.)
   - אחרי ההקמה: להריץ TEST submission אמיתי ולוודא שהמייל מגיע. **לוודא במיוחד** שהתגובה
     נקראת בהצלחה cross-origin (deployment חייב להיות `Who has access: Anyone`).
2. **בחירת סגנון ע"י ראשי הישיבה** — נשלח להם https://orenanox.github.io/yeshiva-landing/styles.html . כשיוחלט: להפוך את הסגנון הנבחר ל-`index.html` (הראשי), ולהשאיר את השאר בארכיון.
3. אופציונלי: דומיין + HTTPS על ה-VPS דרך ה-Caddy הקיים (nanox-caddy) — דורש אישור לקוח לגעת בקונפיג שלו, כרגע לא נגענו.
4. ~~החלפת גופנים לעבים יותר~~ — **בוצע** (Rubik/Assistant בכל הדפים).
5. **תמונות חסרות לאזור הצוות (חוסם שדרוג ויזואלי, לא חוסם שליחה):**
   - **הרב חיים כהן / הרב עמית יוסף / הרב אורי ואננו** — אין שום תמונה אמיתית. כרגע מוצגת
     מונוגרמה מעוצבת, והכרטיס מסומן `data-needs-photo="<שם>"` ב-v11. לחפש `data-needs-photo`.
   - **הרב רפאל רובין** — קיימת `harav-face.jpg` ומוצגת. הלקוח ביקש פורטרט **עם מגבעת ועניבה**;
     אין כזה. לא נוצרה תמונה מלאכותית שלו — אסור להמציא דמות של אדם אמיתי.
   - מפרט לצילומים: חיתוך 1:1, פנים במרכז, תאורה אחידה וחמה, כיפה/מגבעת נראית, רקע נקי.
     לשמור ב-`img/` ולעדכן את ה-`src` בכרטיס ב-v11, ואז `bash tools/regen.sh`.
   - אופציונלי: תמונות נוספות (חדר לימוד, המתחם) כשהלקוח יספק.

### כללים גורפים מהלקוח שחלים גם על דפי הנחיתה (נקבעו 13–16.08, "לעולמי עד")
- **צניעות בכל תמונה שנוצרת ב-AI**: נשים תמיד עם כיסוי ראש (מטפחת שמכסה רק את השיער, צוואר גלוי — לא "hijab look"), לבוש צנוע; באירועים — הפרדה מלאה.
- **כל גבר** — כיפה או מגבעת (חליפה שחורה → מגבעת; חולצה לבנה → כיפה שחורה), ורצוי ציציות נראות.
- **סממנים יהודיים בכל תמונה** (מזוזה, ספרים, מנורה וכו').
- **מיקום הישיבה האמיתי**: רחוב ד"ר ישראל בורגנסקי 7, נתניה — מאחורי בי"ס יונתן. שכונה פנימית על גבעה חולית — **בלי רקעים של ים** בתמונות מבנים.

## 8. הרגלי עבודה עם הלקוח (חשוב לקלוט)

- שפה: עברית. מעדיף שהסוכן מבצע הכול קצה-לקצה ("אתה מבצע הכול, אם חסר משהו — תבקש").
- תמונות: הלקוח מדביק בצ'אט (לא שמיש) — לבקש שישמור קבצים; בפועל הוא שומר אותם על **שולחן העבודה** (כך הגיעו `DSC04301.jpg.jpeg`, `3.JPG.jpeg`, `4.JPG.jpeg`). יש תיקייה מוכנה `Desktop\תמונות-ישיבה` (ריקה בינתיים).
- כל שינוי: לעדכן גם VPS וגם GitHub, ולוודא בפועל (curl / דפדפן) לפני דיווח.
- לינקים להפצה: תמיד לתת את גרסת ה-github.io (HTTPS, כרטיס שיתוף תקין בוואטסאפ). יש תגיות OG בכל הדפים; תמונת השיתוף: `img/harav-og.jpg`. אם כרטיס ישן נתקע בוואטסאפ — להוסיף `?v=N` ללינק.

## 9. לינקים מרוכזים

- עמוד בחירת סגנון (לראשי הישיבה): https://orenanox.github.io/yeshiva-landing/styles.html
- דף הדגל: https://orenanox.github.io/yeshiva-landing/v11.html (וכן v12/v13/v14)
- מאגר: https://github.com/OreNanox/yeshiva-landing
- VPS: http://78.46.133.137:8090/ (אותם נתיבים)
- דור קודם (10 עיצובים): https://orenanox.github.io/yeshiva-landing/index.html
